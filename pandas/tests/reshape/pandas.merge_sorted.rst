pandas.merge_sorted
===================

.. currentmodule:: pandas

Merge multiple sorted DataFrames efficiently
--------------------------------------------

**Signature:**

```python
pandas.merge_sorted(dfs: list[pandas.DataFrame], sort_column: str) -> pandas.DataFrame
